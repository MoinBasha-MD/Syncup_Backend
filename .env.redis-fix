# Add this line to your .env file to disable Redis and suppress connection warnings
REDIS_ENABLED=false

# OR if you want to use Redis, make sure Redis server is running:
# sudo systemctl start redis
# or: docker run -d -p 6379:6379 redis:alpine
